cc_library(
    name = "{{name}}",
    srcs = [
        {{sources}}
    ],
    hdrs = [
        {{headers}}
    ],
    deps = [
        {{deps}}
        {{fidl_deps}}
    ],
    strip_include_prefix = "{{relative_include_dir}}",
)
